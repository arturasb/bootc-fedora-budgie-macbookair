# MacBook keyboard driver configuration (hid_apple)
#
# fnmode=2  — Top row defaults to F1–F12; hold Fn for media keys
# iso_layout=0 — Use ANSI layout (change to 1 if you have an ISO keyboard)
options hid_apple fnmode=2 iso_layout=0
